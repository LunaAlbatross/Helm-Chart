replicaCount: 5

environment: prod

autoscaling:
  enabled: true
  minReplicas: 5
  maxReplicas: 20
  targetCPUUtilizationPercentage: 70

ingress:
  enabled: true

  className: nginx

  hosts:
    - host: payment.example.com
      paths:
        - path: /
          pathType: Prefix

  tls: []
